<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Countries</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .country-card {
            margin-bottom: 30px;
        }
        .flag-img {
            width: 100%;
            height: auto;
            object-fit: cover;
            margin-bottom: 10px;
        }
        .country-info {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">List of Countries</h1>
        
        <div class="row">
            <% data.forEach((item) => { %>
                <div class="col-md-3 country-card">
                    <div class="card border-0">
                        <img src="<%= item.flag %>" alt="<%= item.name %> flag" class="flag-img">
                        <div class="country-info">
                            <h3><%= item.name %></h3>
                            <p><strong>Capital:</strong> <%= item.capital %></p>
                            <p><strong>Region:</strong> <%= item.region %></p>
                            <p><strong>Population:</strong> <%= item.population.toLocaleString() %></p>
                            <p><strong>Area:</strong> <%= item.area ? item.area.toLocaleString() : 'N/A' %></p>
                            <p><strong>CIOC:</strong> <%= item.cioc || 'N/A' %></p>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>